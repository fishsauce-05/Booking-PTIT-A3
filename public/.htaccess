Options -Indexes
RewriteEngine On

# Cho phép truy cập file/thư mục thật
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tất cả request còn lại → index.php
RewriteRule ^ index.php [L]
